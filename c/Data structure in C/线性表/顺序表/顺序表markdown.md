1. " -> "  : 专门用于结构体指针，通过指针访问结构体里面的成员变量
2. " . " : 用于普通结构体变量
3. 指定位置插入元素：
i=L->length  表的最大值
 L->data[i+1] = L->data[i];   //往后挪一个位置
4. 指定位置删除元素 ：
i=pos位置
L->data[i-1]  =  L->data[i];  //注意pos的逻辑顺序和数组下标
    